/* ===== GLOBAL STYLES ===== */
body {
  margin: 0;
  font-family: 'Arial', sans-serif;
  line-height: 1.6;
  background-color: #f5f5f5;
  color: #222;
  transition: background 0.3s, color 0.3s;
}
a { text-decoration: none; color: inherit; }
h1, h2 { margin: 10px 0; }
section {
  padding: 60px 20px;
  opacity: 0;
  transform: translateY(50px);
  transition: opacity 0.8s ease, transform 0.8s ease;
}
section.visible { opacity: 1; transform: translateY(0); }
.btn { padding: 12px 25px; border-radius: 30px; font-weight: bold; display: inline-block; margin-top: 15px; cursor: pointer; transition: 0.3s; }
.btn:hover { opacity: 0.85; }

/* ===== NAVBAR ===== */
nav {
  position: fixed;
  width: 100%;
  top: 0;
  left: 0;
  background: #fff;
  padding: 15px 30px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  box-shadow: 0 2px 5px rgba(0,0,0,0.1);
  z-index: 1000;
}
nav .logo { font-weight: bold; font-size: 1.5rem; }
nav ul {
  list-style: none;
  display: flex;
  gap: 15px;
  margin: 0;
  padding: 0;
  align-items: center;
}
nav ul li a { font-weight: bold; }
.lang-btn { background: #ff7f50; color: #fff; border: none; padding: 8px 15px; border-radius: 20px; cursor: pointer; transition: 0.3s; }
.lang-btn:hover { opacity: 0.8; }

/* ===== IMAGES ===== */
section img {
  width: 100%;
  max-width: 500px;
  margin-top: 20px;
  border-radius: 15px;
  opacity: 0;
  transform: scale(0.9);
  transition: opacity 0.8s ease, transform 0.8s ease;
}
section.visible img { opacity: 1; transform: scale(1); }

/* ===== SECTIONS ===== */
.section-title { text-align: center; font-size: 2rem; margin-bottom: 30px; }
.section-content { max-width: 900px; margin: auto; }

/* ===== CONTACT ===== */
.contact-form { max-width: 500px; margin: auto; }
.contact-form label { display: block; font-weight: bold; margin-top: 15px; }
.contact-form input, .contact-form textarea {
  width: 100%; padding: 12px; margin-top: 5px;
  border-radius: 15px; border: 1px solid #ccc; background: #f0f0f0;
}
.btn-send { margin-top: 15px; width: 100%; background-color: black; color: white; font-size: 1.1rem; border-radius: 15px; padding: 12px; border: none; cursor: pointer; }
.btn-send:hover { background-color: #333; }
.contact-links { display: flex; justify-content: center; margin-top: 30px; gap: 25px; }
.contact-links .icon { width: 30px; height: 30px; transition: 0.3s; }
.contact-links .icon:hover { transform: scale(1.2); }

/* ===== FOOTER ===== */
footer { text-align: center; padding: 25px 10px; background: #f5f5f5; font-size: 0.9rem; color: #555; }

/* ===== DARK MODE ===== */
.dark-mode { background-color: #222; color: #eee; }
.dark-mode nav { background: #333; }
.dark-mode footer { background: #333; color: #ccc; }
.dark-mode .btn { color: #fff; }
.dark-mode .contact-form input, .dark-mode .contact-form textarea { background: #444; color: #eee; border: 1px solid #666; }
